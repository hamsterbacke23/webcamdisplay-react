// import { fromJS } from 'immutable';
// import { makeSelectWebcamDisplayDomain } from '../selectors';

// const selector = makeSelectWebcamDisplayDomain();

describe('makeSelectWebcamDisplayDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
