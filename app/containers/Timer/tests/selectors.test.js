// import { fromJS } from 'immutable';
// import { makeSelectTimerDomain } from '../selectors';

// const selector = makeSelectTimerDomain();

describe('makeSelectTimerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
