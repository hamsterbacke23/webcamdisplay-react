/*
 * Timer Messages
 *
 * This contains all the text for the Timer component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  header: {
    id: 'app.containers.Timer.header',
    defaultMessage: 'This is Timer container !',
  },
});
